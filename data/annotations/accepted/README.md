# Accepted annotation baseline

The dataset owner accepted all 915 existing manual polygons in CVAT job 2 as correct on 2026-07-31.

The accepted source snapshot remains immutable at:

`../unverified/cvat_job_2_2026-07-30/annotations.xml`

This directory intentionally references that snapshot instead of duplicating or silently rewriting it. Seven zero-polygon frames remain pending annotation; see `../../../outputs/unannotated_cvat_frames.csv`.

