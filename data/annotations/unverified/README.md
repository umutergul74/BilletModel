# Unverified annotation snapshots

This directory contains immutable source exports from CVAT. They are not verified ground truth.

- Never overwrite an original export.
- Automated QA creates warnings and review signals only.
- Human corrections must be made in CVAT and exported as a new, versioned snapshot.
- Promotion to `verified/` requires a completed decision record and structural re-validation.

